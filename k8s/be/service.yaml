apiVersion: v1
kind: Service
metadata:
  name: be
  namespace: demo
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: be
  ports:
    - port: 8080
      targetPort: 8080
