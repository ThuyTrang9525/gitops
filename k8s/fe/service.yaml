apiVersion: v1
kind: Service
metadata:
  name: fe
  namespace: demo
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: NodePort
  selector:
    app: fe
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
